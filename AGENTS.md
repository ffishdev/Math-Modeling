# 华为杯数学建模项目协作约定

## 项目目标与工作原则

- 本工作区用于准备 2026 年华为杯数学建模竞赛，覆盖题目分析、数据审查与预处理、模型选择与验证、代码实现、科研绘图、结果解释和 LaTeX 论文撰写。
- 目标是高作品质量与竞赛表现。
- 以题目要求为依据。区分题目事实、数据观察、建模假设、计算结果和推断；记录关键假设、参数来源、局限性与可复现步骤。
- 优先交付可验证、可复现、可解释的结果。模型复杂度应由任务、数据规模、验证结果和时间约束决定；不因模型新颖而忽略基线、消融、稳健性和误差分析。
- 用户已选择 D 题《山区洪涝灾害下无人机运输与通信协同优化》。问题一“单点往返运输能力与货箱组批方案”已有计算结果和复现说明；后续按题目各问逐步推进。
- 后续题目分析、数据处理、建模、代码、绘图和论文写作，都要结合 `Reference/004-数据建模手册.docx` 与 `Reference/005-近年优秀论文阅读.md` 中的模型适用场景、论证结构、图表做法和验证教训。根据当前题面、数据和竞赛正式要求核验后再采用，不照搬模型组合、经验阈值、图表或结论。
- 本文件可在获得新题面、数据、环境检查或论文阅读证据后小幅更新；记录核验结果，保持与实际工作区一致。

## 核心参考材料使用原则

- 数据建模手册是方法与图表的工具索引：按题型查找评价决策、预测、优化、分类聚类、仿真和图网络方法，以及相应的数据处理和图表建议。手册中的阈值、模型清单和“标准流程”是启发性建议，不是通用定律；必须按题目、数据分布、样本规模和验证结果确认。
- 近年优秀论文及 `Reference/005-近年优秀论文阅读.md` 用于学习逐问组织、从数据到结果的证据链、图表表达和模型论证；也要吸收其中识别出的循环验证、数据泄漏、无标签结果过度解释、模拟结果冒充实测等问题。不得复制论文文字、图件、模型组合或未经核验的数值。
- 出现参考材料与正式题目、原始数据、可复现计算或竞赛正式格式要求不一致时，以后者为准，并记录需要调整的原因。

## 文件范围与目录约定

- 除本文件外，项目文件的新增、编辑、移动和删除原则上限于 `26-Modeling/` 与 `Reference/`；保留原始题目、附件、参考论文和 LaTeX 模板。根目录的 `中国研究生数学建模竞赛操作手册.pdf` 用于报名与提交操作参考。
- `26-Modeling/001-Issue/D题/`：问题一代码当前采用的题面与原始附件。`D题_fish/` 是并列副本；当前两者的数据附件相同、题面文件不同，使用前须明确版本，不能混用。预处理数据另存，不覆盖原始附件。
- `26-Modeling/002-Code/`：分析脚本、Notebook、配置、可复现的中间数据及代码说明。当前问题一在 `002-Code/问题一/`；后续按问题组织，避免复制大体积原始附件。
- `26-Modeling/003-Picture/`：本项目的数据图、图表源文件及必要说明；作为最终图件的主存放位置。
- `26-Modeling/004-Paper/`：LaTeX 模板、论文源码、参考文献、插图与编译产物。现有 `example.tex`、`gmcmthesis.cls`、`gmcm.bst`、`reference.bib`、`figures/` 和 `build/`；保留示例，另建 D 题论文主文件。`figures/` 存放编译所需图片，`build/` 存放生成的 PDF 与辅助文件。
- `Reference/001-近年优秀论文/`：学习论文结构、模型论证、结果呈现和图表质量的案例库；与阅读报告结合使用，识别验证缺陷。
- `Reference/002-绘图/`：可复用的绘图模板、样式示例和科研绘图方法。项目最终图件存入 `26-Modeling/003-Picture/`。
- `Reference/003-参考答案/`：当前为空的辅助参考目录；今后放入材料时先核验与正式题目的对应关系和可信度。
- `Reference/004-数据建模手册.docx`：模型适用场景、数据预处理与配图建议的工具索引；具体方法和经验阈值须按本项目数据核验。
- `Reference/005-近年优秀论文阅读.md`：七篇论文的章节、模型、图表与验证经验总结；后续各阶段都应按任务相关部分查阅。

## 题目与数据工作流

1. 阅读选定题目的完整题面、附件说明和数据文件；将每个子问题拆成目标、输入、约束、输出和评价标准。
2. 先做数据清点和质量审查：记录文件来源、格式、字段、单位、样本量、时间范围、缺失、重复、异常、类别分布及潜在偏差。保留原始数据，所有转换可追溯。
3. 预处理必须由数据含义和题目目标驱动。说明缺失值、异常值、标准化、编码、特征构造、样本筛选等处理规则；先做单位/格式与业务逻辑校验，再判断缺失和异常。异常检测只是标记线索，不得把统计边界外的观测自动认定为错误或删除。
4. 对预测或分类任务，先按时间、对象、设备或采集批次划分训练/验证/测试集，再拟合插补、缩放、编码、特征筛选和阈值；只用训练集拟合这些变换，再应用到保留集。若有重叠窗口、数据增强或合成采样，必须在拆分后仅对训练集执行，避免同源样本跨集合泄漏。纯描述性/评价任务不强行套用预测拆分。
5. 缺失值方法按缺失机制、变量类型、时间/组结构和目标选用删除、中位数/模型插补、时间插值等；若需比较方法，可在独立留出的已观测值上模拟缺失并评估，且遵守原有时间或分组结构。3σ、IQR、缺失比例和 VIF 等经验阈值只作诊断起点，不替代领域判断。
6. 对 D 题各问评估问题是否可解、数据是否足以支撑、核心结果能否验证、实现时间和算力是否可控，以及主要失分风险。结论应基于已检查的题面和数据，不猜测附件内容。
7. 建模先建立合理基线，再逐步加入必要方法。为每个模型说明变量、假设、目标函数/损失、约束、求解方式、参数选择和评价指标，并解释模型为何适用于该子问题。
8. 对预测、优化或分类结果采用与问题相符的验证方案；报告适当的误差指标、基线对比、敏感性/稳健性分析、消融或边界情形。不得只呈现有利结果。

### 模型族选型要点

- 评价决策：AHP 可表达有依据的专家偏好，熵权反映样本中的信息离散度，TOPSIS 给出相对于候选集合的排序；灰色关联、模糊评价或 PCA 仅在其数据条件和解释目标适合时采用。说明指标方向、标准化、权重来源与排序对权重/样本的敏感性。
- 预测：先比较朴素或季节性基线。单变量序列可评估指数平滑、ARIMA 或适用时的 GM(1,1)；多变量结构化数据可比较回归、随机森林或提升树。LSTM 等复杂序列模型需有足够的独立时序样本和时间外验证，不因模型复杂而优先选用。
- 优化：明确决策变量、目标函数、硬约束和可执行方案；线性/整数规划可解时优先作为基线。使用遗传算法、模拟退火、粒子群或 NSGA-II 等启发式算法时，报告可行性、参数、重复运行、收敛/解集质量和选择方案的依据，不把近似解称为已证明的全局最优。
- 分类、聚类、仿真与图网络：分类指标匹配类别不平衡和错误代价；聚类报告尺度处理、簇数依据和稳定性，降维图只作探索；随机仿真报告多次运行分布或不确定区间；图模型明确节点、边、权重和现实含义。

## 代码、环境与复现

- 建模与绘图主要使用 Python、Jupyter Notebook 和按任务需要使用的 MATLAB；论文使用 LaTeX。运行前检查实际可用的解释器、包、内核和排版工具。
- Python 任务优先使用经核验的 `py310` 解释器/Notebook 内核；记录 Python 版本、关键依赖及必要的安装说明。不得擅自修改全局 Conda 配置或其他工作区文件。
- 本机环境检查摘要：Conda 环境 `py310` 位于 `/opt/homebrew/Caskroom/miniconda/base/envs/py310`，解释器为该环境下的 `bin/python`，版本 Python 3.10.21；平台为 macOS 27.0 arm64。
- `py310` 已安装的主要包及版本：NumPy 2.2.5、pandas 2.3.3、SciPy 1.15.3、scikit-learn 1.7.2、Matplotlib 3.10.9、seaborn 0.13.2、JupyterLab 4.6.2、ipykernel 7.3.0、openpyxl 3.1.5。
- 检查时 `py310` 未安装：statsmodels、SymPy、Notebook、XlsxWriter、PuLP、NetworkX、GeoPandas、XGBoost。按具体任务需要再评估和安装，不将其视为基础环境已有依赖。
- 已在 `py310` 注册 `py310` 与 `python3` 两个 Jupyter 内核；`conda run -n py310 jupyter kernelspec list` 可列出它们，`conda run -n py310 jupyter lab --version` 返回 4.6.2。默认 zsh 会话中没有 `jupyter` 命令；当前 `python` 与 `python3` 均为 `/Users/fish/.local/bin/` 下的 Python 3.12.14，且 `py310` 未激活。因此运行项目 Python 代码时应显式激活 `py310` 或使用 `conda run -n py310 ...`，并在 Notebook 中检查 `sys.executable` 确认内核。
- 将探索性分析与可复现的最终流程分开整理。脚本和 Notebook 应有清晰入口、相对路径、必要注释、固定随机种子（适用时）和明确输出；避免依赖个人机器上的绝对路径。
- 输出数据、图表和论文数值应能追溯到代码与输入。重要结果提供运行命令、参数或 Notebook 执行顺序；避免手工改写计算结果而不同步更新来源。

## 科研绘图

- 图表应服务于论证，优先展示数据结构、方法流程、模型对比、误差/残差、敏感性、优化方案或关键机制；避免装饰性图表和重复表达。
- 绘图前建立“结论—所需证据—图表—数据/代码来源”对应关系。评价题可用权重与得分排序图；预测题用测试集预测/观测、残差和适当的不确定区间；优化题用可行方案、Pareto 前沿或收敛/约束检查；分类题用混淆矩阵及适用的 ROC/PR 指标；聚类题用质量指标配合标注清楚的探索图；仿真题展示多次轨迹与区间。
- 流程图说明方法，不证明方法有效；热图和 t-SNE 等降维图不替代独立验证。所有结果图由真实数据和可追溯代码生成，比较图统一尺度、色标和单位；地图保留比例/坐标信息，图题与图注说明样本范围和必要的解释。
- 全项目绘图总则、四种工具的适用边界、优秀论文图例和交付核对表见 `Reference/002-绘图/绘图策略.md`；每个题目开始绘图前按实际任务建立“结论—证据—图表—数据/代码来源”清单。
- 定量结果图由真实输入数据和可追溯的 Python/MATLAB 脚本生成。GPT Image 和 PowerPoint 可用于概念示意、方法流程或可编辑版面，不得生成或手工改写实证数值、曲线、坐标和模型表现；概念图需核对关系并标明示意属性。
- 可按任务使用 `nature-figure` 制作与质检论文级 Python/R 科研图；它不替代建模和独立验证。使用时读取当时可用的技能说明，并遵循其后端选择与同一后端出图、预览、导出及质检要求；MATLAB 项目按其自身流程制图。
- `nature-figure` 工作流应先界定单句结论、证据层级、面板角色和最终版面尺寸；多面板图检查对齐与文字碰撞，并在最终 PDF 的实际尺寸复核。全项目最终图件及源文件位置按目录约定执行；通用图形模板存入 `Reference/002-绘图/`。
- 选择适合内容的静态科研图、矢量图或流程图。统一字体、字号、线宽、配色、图例、单位、精度和留白；保证中文可读、缩放后文字清晰。
- 每幅图应有编号、准确的中英文/中文图题（按论文风格统一）、单位和必要图注；在正文中解释其支持的结论。不得仅凭图形外观作超出数据的因果解释。
- 最终图件保存至 `26-Modeling/003-Picture/`，可复用的模板和样式示例保存至 `Reference/002-绘图/`。保留生成图件的源代码或可编辑文件；论文编译需要时将对应成图放入 `26-Modeling/004-Paper/figures/`。

## LaTeX 论文写作与交付

- 先阅读题目要求、`26-Modeling/004-Paper/` 中的 LaTeX 模板、数据建模手册和若干近年优秀论文，再确定目录、图表风格和论证顺序。参考论文用于学习，不照抄内容、措辞、模型组合或图表。
- 以 `example.tex`、`gmcmthesis.cls`、`gmcm.bst` 和 `reference.bib` 为模板基础，另建 D 题论文源码；示例中的折叠桌内容、队伍信息、关键词、图表和引用均须替换为经核验的本题内容。模板说明与当年正式竞赛要求冲突时，以正式要求为准。
- 论文围绕题目逐问回答，形成“问题重述—分析—假设与符号—模型建立—求解/算法—结果与验证—讨论—优缺点与改进”的完整证据链；具体章节按题目要求与篇幅调整。
- 先整理可复核的结果和图表，再撰写正文，最后核对摘要与结论；每个重要图表在正文中引用并解释，写清数据来源、单位和适用范围，避免图表重复表达同一结果。
- 用户期望正文（不含附件）约 60 页。把它作为规划目标而非凑页指标：以正式竞赛格式、内容完整性和可读性为先，不靠重复文字、空图或无关综述扩页。
- 对关键结论给出可复核的量化依据。符号、公式、单位、变量命名、图表编号、交叉引用、参考文献和摘要中的数字应前后一致；引用应可查证且规范。
- 表格优先使用模板已有的 `booktabs` 三线表命令；表题置上，图题置下。公式使用 `equation`、`align` 等环境。图表、公式和章节使用 `\label`、`\ref`、`\eqref` 维护编号与正文引用，避免手工编号。
- 模板类文件要求 XeLaTeX；当前 `example.tex` 指定 `fontset=windows`，在 macOS 上需核验字体，必要时在论文副本中使用 `fontset=mac`。模板使用 BibTeX、`gmcm.bst` 与 `reference.bib` 管理参考文献；正文引用、`.bib` 条目和文末列表要一致。
- 从 `26-Modeling/004-Paper/` 运行 `latexmk -xelatex -outdir=build <主文件>.tex`，将编译产物集中在 `build/`。每次交付前检查编译错误、缺失引用、溢出警告和最终 PDF 的逐页版面，核对中文字体、目录、公式、三线表、图表清晰度、页眉页脚及参考文献。
- 不把未执行的分析写成已完成，不把模拟数据伪称实测数据，不隐藏方法失效、数据限制或不确定性。

## 协作与汇报

- 大任务按阶段推进：题目判定 → 数据审查 → 方法设计 → 实现与验证 → 绘图 → 论文整合与排版。每阶段留下可检查的文件或结论，再进入下一阶段。
- 对不确定事项，先检查现有题面、附件、说明和项目文件；可以在不依赖答案的部分继续推进。只有会实质影响方法或交付的选择无法从现有资料判断时，再向用户提出简短问题。
- 汇报时简洁说明改动位置、方法选择依据、已完成的验证及尚存限制。区分建议、计划和已执行结果。
